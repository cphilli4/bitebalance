import {
  SafeAreaView,
} from "react-native";

import Calendar from "@/components/MealCalendar"

export default function CalendarPage() {
  return (
    <SafeAreaView>
      <Calendar />
    </SafeAreaView>
  );
}

